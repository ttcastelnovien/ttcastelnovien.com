services:
  php:
    build:
      context: .
      target: prod
      dockerfile: frankenphp/dockerfile
    restart: always
    volumes:
      - "$PWD/secrets:/app/secrets"
  
  database:
    restart: always
  
  cache:
    restart: always
  
  gotenberg:
    restart: always
